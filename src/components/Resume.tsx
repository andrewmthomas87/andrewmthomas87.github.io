import * as React from 'react'

import s from './resume.less'

function Resume(): React.ReactElement {
	return <a className={s.resume} href='Andrew-Thomas-Resume.pdf'>View Resume</a>
}

export default Resume
